// Code generated by ent, DO NOT EDIT.

package ent

import (
	"eino-quickstart/ent/agentknowledgebase"
	"eino-quickstart/ent/knowledgebase"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// AgentKnowledgeBase is the model entity for the AgentKnowledgeBase schema.
type AgentKnowledgeBase struct {
	config `json:"-"`
	// ID of the ent.
	ID uint64 `json:"id,omitempty"`
	// Subject holds the value of the "subject" field.
	Subject string `json:"subject,omitempty"`
	// KnowledgeBaseID holds the value of the "knowledge_base_id" field.
	KnowledgeBaseID uint64 `json:"knowledge_base_id,omitempty"`
	// CreatedBy holds the value of the "created_by" field.
	CreatedBy string `json:"created_by,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the AgentKnowledgeBaseQuery when eager-loading is set.
	Edges        AgentKnowledgeBaseEdges `json:"edges"`
	selectValues sql.SelectValues
}

// AgentKnowledgeBaseEdges holds the relations/edges for other nodes in the graph.
type AgentKnowledgeBaseEdges struct {
	// KnowledgeBase holds the value of the knowledge_base edge.
	KnowledgeBase *KnowledgeBase `json:"knowledge_base,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// KnowledgeBaseOrErr returns the KnowledgeBase value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e AgentKnowledgeBaseEdges) KnowledgeBaseOrErr() (*KnowledgeBase, error) {
	if e.KnowledgeBase != nil {
		return e.KnowledgeBase, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: knowledgebase.Label}
	}
	return nil, &NotLoadedError{edge: "knowledge_base"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*AgentKnowledgeBase) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case agentknowledgebase.FieldID, agentknowledgebase.FieldKnowledgeBaseID:
			values[i] = new(sql.NullInt64)
		case agentknowledgebase.FieldSubject, agentknowledgebase.FieldCreatedBy:
			values[i] = new(sql.NullString)
		case agentknowledgebase.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the AgentKnowledgeBase fields.
func (_m *AgentKnowledgeBase) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case agentknowledgebase.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = uint64(value.Int64)
		case agentknowledgebase.FieldSubject:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field subject", values[i])
			} else if value.Valid {
				_m.Subject = value.String
			}
		case agentknowledgebase.FieldKnowledgeBaseID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field knowledge_base_id", values[i])
			} else if value.Valid {
				_m.KnowledgeBaseID = uint64(value.Int64)
			}
		case agentknowledgebase.FieldCreatedBy:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field created_by", values[i])
			} else if value.Valid {
				_m.CreatedBy = value.String
			}
		case agentknowledgebase.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the AgentKnowledgeBase.
// This includes values selected through modifiers, order, etc.
func (_m *AgentKnowledgeBase) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryKnowledgeBase queries the "knowledge_base" edge of the AgentKnowledgeBase entity.
func (_m *AgentKnowledgeBase) QueryKnowledgeBase() *KnowledgeBaseQuery {
	return NewAgentKnowledgeBaseClient(_m.config).QueryKnowledgeBase(_m)
}

// Update returns a builder for updating this AgentKnowledgeBase.
// Note that you need to call AgentKnowledgeBase.Unwrap() before calling this method if this AgentKnowledgeBase
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *AgentKnowledgeBase) Update() *AgentKnowledgeBaseUpdateOne {
	return NewAgentKnowledgeBaseClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the AgentKnowledgeBase entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *AgentKnowledgeBase) Unwrap() *AgentKnowledgeBase {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: AgentKnowledgeBase is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *AgentKnowledgeBase) String() string {
	var builder strings.Builder
	builder.WriteString("AgentKnowledgeBase(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("subject=")
	builder.WriteString(_m.Subject)
	builder.WriteString(", ")
	builder.WriteString("knowledge_base_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.KnowledgeBaseID))
	builder.WriteString(", ")
	builder.WriteString("created_by=")
	builder.WriteString(_m.CreatedBy)
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// AgentKnowledgeBases is a parsable slice of AgentKnowledgeBase.
type AgentKnowledgeBases []*AgentKnowledgeBase
