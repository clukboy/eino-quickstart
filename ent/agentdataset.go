// Code generated by ent, DO NOT EDIT.

package ent

import (
	"eino-quickstart/ent/agentdataset"
	"eino-quickstart/ent/dataset"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// AgentDataset is the model entity for the AgentDataset schema.
type AgentDataset struct {
	config `json:"-"`
	// ID of the ent.
	ID uint64 `json:"id,omitempty"`
	// Subject holds the value of the "subject" field.
	Subject string `json:"subject,omitempty"`
	// DatasetID holds the value of the "dataset_id" field.
	DatasetID uint64 `json:"dataset_id,omitempty"`
	// CreatedBy holds the value of the "created_by" field.
	CreatedBy string `json:"created_by,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the AgentDatasetQuery when eager-loading is set.
	Edges        AgentDatasetEdges `json:"edges"`
	selectValues sql.SelectValues
}

// AgentDatasetEdges holds the relations/edges for other nodes in the graph.
type AgentDatasetEdges struct {
	// Dataset holds the value of the dataset edge.
	Dataset *Dataset `json:"dataset,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// DatasetOrErr returns the Dataset value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e AgentDatasetEdges) DatasetOrErr() (*Dataset, error) {
	if e.Dataset != nil {
		return e.Dataset, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: dataset.Label}
	}
	return nil, &NotLoadedError{edge: "dataset"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*AgentDataset) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case agentdataset.FieldID, agentdataset.FieldDatasetID:
			values[i] = new(sql.NullInt64)
		case agentdataset.FieldSubject, agentdataset.FieldCreatedBy:
			values[i] = new(sql.NullString)
		case agentdataset.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the AgentDataset fields.
func (_m *AgentDataset) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case agentdataset.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = uint64(value.Int64)
		case agentdataset.FieldSubject:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field subject", values[i])
			} else if value.Valid {
				_m.Subject = value.String
			}
		case agentdataset.FieldDatasetID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field dataset_id", values[i])
			} else if value.Valid {
				_m.DatasetID = uint64(value.Int64)
			}
		case agentdataset.FieldCreatedBy:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field created_by", values[i])
			} else if value.Valid {
				_m.CreatedBy = value.String
			}
		case agentdataset.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the AgentDataset.
// This includes values selected through modifiers, order, etc.
func (_m *AgentDataset) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryDataset queries the "dataset" edge of the AgentDataset entity.
func (_m *AgentDataset) QueryDataset() *DatasetQuery {
	return NewAgentDatasetClient(_m.config).QueryDataset(_m)
}

// Update returns a builder for updating this AgentDataset.
// Note that you need to call AgentDataset.Unwrap() before calling this method if this AgentDataset
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *AgentDataset) Update() *AgentDatasetUpdateOne {
	return NewAgentDatasetClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the AgentDataset entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *AgentDataset) Unwrap() *AgentDataset {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: AgentDataset is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *AgentDataset) String() string {
	var builder strings.Builder
	builder.WriteString("AgentDataset(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("subject=")
	builder.WriteString(_m.Subject)
	builder.WriteString(", ")
	builder.WriteString("dataset_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.DatasetID))
	builder.WriteString(", ")
	builder.WriteString("created_by=")
	builder.WriteString(_m.CreatedBy)
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// AgentDatasets is a parsable slice of AgentDataset.
type AgentDatasets []*AgentDataset
